# 高token项目交接协议 (2026-08-18 用户指令)

## 规则 (用户原话要点)
"标记，高token项目，在双倍时间暂停并交接到新窗口等低token重新开始。
所有任务只有紧急的状态下才使用高token。所有任务主要在低token期间完成任务。
因为我们有很多时间而且并不着急。"

## 落地流程
1. 收到暂停指令 → 立即停止一切重token活 (回测验收/深读/分析/长报告)
2. 现场落盘 (必经三处):
   - `00_<项目>现场进度.md` 文件头加 `⚠️ 状态: 高token项目·双倍时段暂停 (时间 用户指令)` + `上次推进: <时间>`
   - 待办区: 把中断点写成**可直接续接**的步骤 — 明确: 已完成的✅ / 卡在哪 / 续接第一步的具体命令 / 预期结果参考值(标注"勿采信/待验证")
   - 环境备忘保持最新 (Chrome 9222、脚本路径、数据目录)
3. 向用户一句话确认: 已暂停+交接位置+低token时段可续接的事项
4. 续接时: 新窗口先读现场文件 → 从待办区断点开始 → 全程低token

## 关键教训 (2026-08-18 模型C实测)
- **作废数字显式标注**: 浅目录回测结果 23352/0.72% 已跑出但被证伪 → 现场文件写 "作废待重跑" + 原因(BT_KLINE_DIR) + 正确命令, 防新窗口误采信
- **预期结果参考值也写**: 即使无效, 保留"浅目录版参考"供续接时对比方向, 但必须标 "勿采信"
- 暂停前把 cc/子代理的后台任务状态一并落盘 (交付物路径、py_compile/自检结果、待验收项)
